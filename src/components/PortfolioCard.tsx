import { ProjectDataType } from "./PortfolioItem";

export default function PortfolioCard({ project }: { project: ProjectDataType | null }) {
  return (
    <div className="bg-black h-full w-5/12 rounded-xl"></div>
  );
}